/**
 * EduCore - Teacher's AI Assistant Platform
 *
 * This is a router-based application. All routing configuration
 * is handled in main.tsx with react-router-dom.
 *
 * The app automatically routes to:
 * - /login for authentication
 * - / for dashboard (protected)
 * - /lessons for lesson generation
 * - /exams for exam builder
 * - /marking for paper scanner
 * - /analytics for performance dashboard
 * - /settings for app settings
 * - /financial/* for financial modules
 */

// Router handles all rendering in main.tsx
export default function App() {
  return null;
}
